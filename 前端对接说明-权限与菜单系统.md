# 前端对接说明 — 权限与菜单系统

> 版本：1.0 | 日期：2026-07-03 | 关联后端变更：[权限管理模块完整变更日志](file:///d:/java_program/SmartLightingExp/docs/权限管理模块完整变更日志.md)

---

## 一、导航菜单 — 不再硬编码

### 旧方式（废弃）
```javascript
// 前端硬编码导航栏 ← 不再使用
const navItems = [
  { name: 'dashboard', label: '数字孪生', icon: 'grid', path: '/dashboard' },
  { name: 'devices',   label: '设备管理', icon: 'bulb', path: '/devices' },
  // ...
]
```

### 新方式（动态获取）
```javascript
// 方式1：登录接口直接返回菜单
const loginRes = await axios.post('/api/auth/login', { username, password })
// loginRes.data.menus → 树形结构，直接渲染导航栏

// 方式2：页面刷新时通过 /me 重新获取
const meRes = await axios.get('/api/auth/me')
// meRes.data.menus      → 可见菜单树（前端导航渲染用）
// meRes.data.permissions → 权限编码列表（按钮级权限用）
```

### 前端渲染示例

```javascript
// 递归渲染菜单树
function renderMenu(menus) {
  return menus.map(menu => ({
    key: menu.id,
    label: menu.name,
    icon: menu.icon,
    path: menu.path,
    children: menu.children && menu.children.length > 0
      ? renderMenu(menu.children)
      : undefined,
    // 若有子节点，本身不跳转，折叠展开
  }))
}
```

### 菜单结构对照

```
一级菜单（11 个）
├── 数字孪生  → /dashboard
├── 设备管理  → /devices
├── 数据报表  → /analytics
├── 能耗走势  → /energy
├── 告警中心  → /warning
├── 策略配置  → /strategy
├── 智能助手  → /assistant
├── 系统日志  → /logs
├── 用户管理  → /users
└── 系统管理  → /system （容器菜单，不跳转）
    ├── 权限管理 → /system/permission
    └── 菜单管理 → /system/menu
```

---

## 二、按钮级权限控制

### 接口返回的权限列表

```javascript
// /api/auth/me 返回示例
{
  "username": "admin",
  "roleName": "超级管理员",
  "permissions": [
    "dashboard:read",
    "device:read", "device:create", "device:update", "device:delete", "device:control",
    "telemetry:read",
    "energy:read",
    "alarm:read", "alarm:handle", "alarm:delete",
    "policy:read", "policy:create", "policy:update", "policy:delete",
    "assistant:read",
    "audit:read",
    "user:read", "user:create", "user:update", "user:delete",
    "permission:read", "permission:create", "permission:update", "permission:delete",
    "menu:read", "menu:create", "menu:update", "menu:delete"
    // role:assign 等
  ]
}
```

### 按钮级显示/隐藏

```javascript
// 全局可用 userPermissions
const userPermissions = meRes.data.permissions || []

// 工具函数
function hasPermission(code) {
  return userPermissions.includes(code)
}

// 使用示例
{hasPermission('device:create') && <Button>新增设备</Button>}
{hasPermission('device:delete') && <Button>删除</Button>}
{hasPermission('alarm:handle') && <Button>处理告警</Button>}
{hasPermission('user:create') && <Button>新增用户</Button>}
```

### 权限编码速查表

| 模块 | 操作 | 编码 |
|:----|:-----|:-----|
| 数字孪生 | 查看大屏 | `dashboard:read` |
| 设备管理 | 查看 | `device:read` |
| 设备管理 | 新增 | `device:create` |
| 设备管理 | 编辑 | `device:update` |
| 设备管理 | 删除 | `device:delete` |
| 设备管理 | 控制 | `device:control` |
| 数据报表 | 查看 | `telemetry:read` |
| 能耗走势 | 查看 | `energy:read` |
| 告警中心 | 查看 | `alarm:read` |
| 告警中心 | 处理 | `alarm:handle` |
| 告警中心 | 删除 | `alarm:delete` |
| 策略配置 | 查看 | `policy:read` |
| 策略配置 | 新增 | `policy:create` |
| 策略配置 | 编辑 | `policy:update` |
| 策略配置 | 删除 | `policy:delete` |
| 智能助手 | 使用 | `assistant:read` |
| 系统日志 | 查看 | `audit:read` |
| 用户管理 | 查看 | `user:read` |
| 用户管理 | 新增 | `user:create` |
| 用户管理 | 编辑 | `user:update` |
| 用户管理 | 删除 | `user:delete` |
| 权限管理 | 查看 | `permission:read` |
| 权限管理 | 新增 | `permission:create` |
| 权限管理 | 编辑 | `permission:update` |
| 权限管理 | 删除 | `permission:delete` |
| 菜单管理 | 查看 | `menu:read` |
| 菜单管理 | 新增 | `menu:create` |
| 菜单管理 | 编辑 | `menu:update` |
| 菜单管理 | 删除 | `menu:delete` |

---

## 三、403 权限不足处理

当用户调用其角色无权限的接口时，后端返回：

```json
{
  "code": 403,
  "message": "权限不足，需要 device:create",
  "data": null
}
```

前端应统一拦截 403：

```javascript
// axios 响应拦截器
axios.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 403) {
      // 提示"权限不足"或直接隐藏操作入口
      message.warning(error.response.data.message || '权限不足')
    }
    if (error.response?.status === 401) {
      // Token 过期或无效，跳转登录页
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

---

## 四、各角色可见功能速查

| 功能模块 | 超级管理员 | 市政人员 | 路灯管理员 | 应急人员 |
|:--------|:---------:|:--------:|:---------:|:--------:|
| 数字孪生 | ✅ | ✅ 查看 | ✅ 查看 | ✅ 查看 |
| 设备管理 | ✅ **全部** | ✅ 查看+控制 | ✅ **全部** | ❌ |
| 数据报表 | ✅ | ✅ | ✅ | ❌ |
| 能耗走势 | ✅ | ✅ | ✅ | ❌ |
| 告警中心 | ✅ **全部** | ✅ 查看+处理 | ✅ **全部** | ✅ 仅查看 |
| 策略配置 | ✅ **全部** | ✅ 仅查看 | ✅ 仅查看 | ❌ |
| 智能助手 | ✅ | ✅ | ✅ | ✅ |
| 系统日志 | ✅ | ✅ | ❌ | ❌ |
| 用户管理 | ✅ **全部** | ❌ | ❌ | ❌ |
| 系统管理→权限管理 | ✅ | ❌ | ❌ | ❌ |
| 系统管理→菜单管理 | ✅ | ❌ | ❌ | ❌ |

> 说明：✅ = 可见，❌ = 导航栏隐藏且 API 返回 403

---

## 五、新增接口汇总

### 菜单管理

| 功能 | 方法 | 路径 | 说明 |
|:----|:----|:-----|:------|
| 完整菜单树 | `GET` | `/api/menus/tree` | 后台管理用（需 `menu:read`） |
| 可见菜单树 | `GET` | `/api/menus/visible` | **前端导航用**（无需权限，自动过滤） |
| 菜单列表 | `GET` | `/api/menus` | 扁平列表（需 `menu:read`） |
| 新增菜单 | `POST` | `/api/menus` | 需 `menu:create` |
| 修改菜单 | `PUT` | `/api/menus/{id}` | 需 `menu:update` |
| 删除菜单 | `DELETE` | `/api/menus/{id}` | 需 `menu:delete` |

### 权限管理

| 功能 | 方法 | 路径 | 说明 |
|:----|:----|:-----|:------|
| 权限树 | `GET` | `/api/permissions/tree?roleId=` | 含 checked 标记（需 `permission:read`） |
| 权限列表 | `GET` | `/api/permissions` | 扁平列表（需 `permission:read`） |
| 新增权限 | `POST` | `/api/permissions` | 需 `permission:create` |
| 修改权限 | `PUT` | `/api/permissions/{id}` | 需 `permission:update` |
| 删除权限 | `DELETE` | `/api/permissions/{id}` | 需 `permission:delete` |

### 角色管理（新增权限分配）

| 功能 | 方法 | 路径 | 说明 |
|:----|:----|:-----|:------|
| 分配权限 | `PUT` | `/api/roles/{id}/permissions` | Body: `{"permissionIds":[1,2,3]}`，需 `role:assign` |
| 所有权限 | `GET` | `/api/roles/permissions` | 供下拉选择（无需权限） |

### 认证接口（新增字段）

| 接口 | 新增返回字段 |
|:----|:------------|
| `POST /api/auth/login` | `permissions` + `menus` |
| `GET /api/auth/me` | `permissions` + `menus` |

---

## 六、动态菜单配置流程（超级管理员操作）

```
登录(SUPER_ADMIN) → 看到"系统管理→菜单管理"
  → 新增菜单
    - 名称：照明节能
    - 路径：/energy-save
    - 组件：energy-save/index
    - 关联权限编码：energy-save
  → 权限管理 → 新增模块权限 energy-save (MODULE)
  → 权限管理 → 新增操作权限 energy-save:read (ACTION, parent=energy-save)
  → 角色管理 → SUPER_ADMIN → 分配权限 → 勾选新权限
  → 刷新 → 导航栏出现"照明节能"
  → 再将新权限分配给其他角色
```

全程无需修改前端代码。

---

## 七、登录/用户信息对接示例

### 完整 login 调用

```javascript
async function login(username, password) {
  const res = await axios.post('/api/auth/login', { username, password })
  
  const { token, username, roleName, roleCode, permissions, menus } = res.data
  
  // 保存 token
  localStorage.setItem('token', token)
  
  // 保存用户信息和权限
  localStorage.setItem('userPermissions', JSON.stringify(permissions))
  localStorage.setItem('userMenus', JSON.stringify(menus))
  
  // 渲染导航栏
  renderSidebar(menus)
  
  return res.data
}
```

### 页面刷新恢复

```javascript
async function restoreSession() {
  const token = localStorage.getItem('token')
  if (!token) return false
  
  try {
    axios.defaults.headers.common['Authorization'] = `Bearer ${token}`
    const res = await axios.get('/api/auth/me')
    
    const { username, roleName, roleCode, permissions, menus } = res.data
    
    // 更新缓存
    localStorage.setItem('userPermissions', JSON.stringify(permissions))
    localStorage.setItem('userMenus', JSON.stringify(menus))
    
    // 重新渲染导航栏
    renderSidebar(menus)
    
    return true
  } catch (e) {
    // Token 无效，跳转登录
    localStorage.clear()
    window.location.href = '/login'
    return false
  }
}
```

# 智慧路灯节能系统 — 大屏展示适配方案

## 一、概述

### 1.1 目标

在不改动现有代码 px 体系的前提下，通过 `transform: scale()` 整体缩放方案，使系统在 **2K（2560×1440）和 4K（3840×2160）** 等大屏分辨率下等比放大，呈现驾驶舱大屏效果。

### 1.2 设计基准

| 项目 | 值 |
|------|-----|
| 设计基准分辨率 | **1920 × 1080**（当前适配宽度） |
| 缩放策略 | `transform: scale(ratio)` 整体等比缩放 |
| 对齐方式 | 水平居中，垂直顶部对齐 |
| 缩放对象 | RouterView 主体内容（Login 页除外） |

### 1.3 适配效果预期

| 分辨率 | 缩放比 | 效果 |
|--------|--------|------|
| 1920×1080 | 1.0× | 完全不变，回归正常 |
| 2560×1440 | 1.333× | 内容等比放大，布局比例一致 |
| 3840×2160 | 2.0× | 内容等比放大，字体图表清晰可读 |
| 1366×768 | 0.712× | 等比缩小，内容完整可访问 |

---

## 二、整体架构

```
index.html
  └── #app
       └── <ScreenScale designWidth="1920">          ← 新增：缩放容器
            ├── <RouterView />                        ← 页面内容（被缩放）
            └── <MockIndicator />
```

ScreenScale 组件负责：
- 监听 `window.resize` 计算缩放比
- 通过 CSS `transform: scale()` 缩放内部内容
- 居中定位，两侧留白
- 通知子组件（ECharts 等）缩放比例

---

## 三、新增文件清单

### 3.1 `src/components/ScreenScale.vue`

通用缩放容器组件。

**Props：**

| 名称 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `designWidth` | Number | `1920` | 设计稿基准宽度（px） |
| `maxScale` | Number | `2` | 最大缩放倍数，防止超宽屏过大 |
| `disabled` | Boolean | `false` | 禁用缩放，透传渲染 |
| `excludePaths` | Array | `['/login']` | 不需要缩放的路由路径 |

**核心逻辑：**

```
onMounted / onResize → 计算 ratio = Math.min(window.innerWidth / designWidth, maxScale)
                       → 设置 wrapper 样式居中
                       → 设置 content 的 transform: scale(ratio)
                       → 通过 provide/inject 传递 scaleRatio 给子组件
```

**样式要点：**

```css
.screen-scale-wrapper {
  width: 100%;
  height: 100%;
  overflow: hidden auto;     /* 内容超出时允许滚动 */
  display: flex;
  justify-content: center;   /* 水平居中 */
}

.screen-scale-content {
  width: 1920px;             /* 锁死设计宽度 */
  height: 100vh;
  transform-origin: top center;
  overflow: hidden;
}
```

### 3.2 `src/composables/useChartScale.js`

ECharts 缩放工具 composable，解决 ECharts 中 fontSize 写死 px 的问题。

**提供的能力：**

```js
const { scaleRatio, scaleVal, scaleOption, onScaleChange } = useChartScale(baseWidth = 1920)
```

| 导出 | 类型 | 说明 |
|------|------|------|
| `scaleRatio` | Ref\<number\> | 当前缩放比，如 1.333 |
| `scaleVal(val)` | Function | 按缩放比放大数值：`scaleVal(10)` → `13` |
| `scaleOption(option)` | Function | 递归遍历 ECharts option，放大所有 `fontSize` / `height` / `width` / `padding` / `borderRadius` 等数值 |
| `onScaleChange(cb)` | Function | 注册缩放比变化回调，返回取消监听函数 |

**示例：**

```js
// 之前（写死 px）
chart.setOption({
  xAxis: { axisLabel: { fontSize: 10 } },
  yAxis: { axisLabel: { fontSize: 10 } },
})

// 之后（自适应）
const { scaleVal } = useChartScale()
chart.setOption({
  xAxis: { axisLabel: { fontSize: scaleVal(10) } },
  yAxis: { axisLabel: { fontSize: scaleVal(10) } },
})
```

或者批量处理：

```js
const { scaleOption } = useChartScale()
chart.setOption(scaleOption({
  // 原样写，运行时自动放大所有数值
  xAxis: { axisLabel: { fontSize: 10 } },
  ...
}))
```

---

## 四、修改文件清单

### 4.1 `src/App.vue`

引入 ScreenScale 包裹路由内容。

```diff
 <template>
-  <RouterView />
-  <MockIndicator />
+  <ScreenScale>
+    <RouterView />
+    <MockIndicator />
+  </ScreenScale>
 </template>

 <script setup>
+import ScreenScale from './components/ScreenScale.vue'
 import MockIndicator from './components/MockIndicator.vue'
 </script>
```

### 4.2 `src/views/Dashboard.vue`

**改动项：**

| 位置 | 改动内容 |
|------|----------|
| script 引入 | 引入 `useChartScale` |
| ECharts init | 所有 `fontSize` 写死值改为 `scaleVal(n)` |
| ECharts `grid` | `top/bottom/left/right` 改为 `scaleVal(n)` |
| 统计卡片 | `font-size: 26px` → 通过 CSS 变量动态控制 |
| chart-area | `height: 220px` → `height: calc(220px * var(--scale-ratio))` |
| 主区域 Grid | `1fr 320px` → `1.5fr 1fr`（大屏下分区面板占比放大） |
| 快速入口按钮 | 间距和字号按比例放大 |

### 4.3 `src/views/Analytics.vue`

**改动项：**

| 位置 | 改动内容 |
|------|----------|
| script 引入 | 引入 `useChartScale` |
| ECharts 柱状图 | 所有 `fontSize` / `grid` 值使用 `scaleVal()` |
| ECharts 饼图 | 同上 |
| 指标卡片 Grid | `repeat(4,1fr)` 不变，gap 增大 |
| 图表 Grid | `2fr 1fr` 不变 |
| chart-area-lg | `height: 260px` → `height: calc(260px * var(--scale-ratio))` |

### 4.4 `src/views/EnergyTrend.vue`

**改动项：**

| 位置 | 改动内容 |
|------|----------|
| script 引入 | 引入 `useChartScale` |
| ECharts 配置 | 所有 `fontSize` / `grid` 值使用 `scaleVal()` |
| chart-area | `height: 360px` → `height: calc(360px * var(--scale-ratio))` |

### 4.5 `src/three/core/index.js`

**改动项：**

```diff
 const onResize = () => {
   const w = container.clientWidth
   const h = container.clientHeight
-  const aspect = w / h
-  camera.aspect = aspect
+  camera.aspect = w / h
   camera.updateProjectionMatrix()
   renderer.setSize(w, h)
+
+  // 大屏下适当拉远相机，展示更广场景
+  const ratio = w / 1920
+  if (ratio > 1.1) {
+    camera.position.set(5 * ratio, 5, 10 * ratio)
+    camera.lookAt(0, 0, 0)
+  }
 }
```

### 4.6 `src/layouts/MainLayout.vue`

**改动项：**

| 位置 | 改动内容 |
|------|----------|
| sidebar width | `200px` → `clamp(200px, 12vw, 260px)`，在超宽屏下适当扩展 |
| brand-title | 字号从 `13px` → `clamp(13px, 0.8vw, 16px)` |
| nav-label | 字号从 `13px` → `clamp(13px, 0.7vw, 15px)` |

---

## 五、边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 窗口 < 1920 | Scale ratio < 1，等比缩小，底部可能出现滚动 |
| 窗口 resize | debounce 100ms 后重新计算 scale 值 |
| Element Plus Dialog | Dialog 挂载在 body 上不受 scale 影响，通过 ScreenScale provide 的 scaleRatio 调整 Dialog 宽度 |
| 极宽屏（>3840） | 通过 `maxScale` prop 限制最大缩放倍数（默认 2） |
| 浏览器 zoom 叠加 | window.devicePixelRatio 会变化，ScreenScale 忽略之，保持 CSS 缩放独立 |

---

## 六、验收标准

| 分辨率 | 测试页面 | 通过条件 |
|--------|----------|----------|
| 1920×1080 | 全站 | 和当前版本完全一致，回归通过 |
| 2560×1440 | Dashboard / Analytics / EnergyTrend | 内容等比放大 1.33×，ECharts 标注清晰 |
| 3840×2160 | Dashboard / Analytics / EnergyTrend | 内容等比放大 2×，无模糊，3D 场景正常 |
| 1366×768 | 全站 | 内容等比缩小，可滚动访问 |

---

## 七、文件改动汇总

| 操作 | 文件 | 说明 |
|------|------|------|
| **新增** | `src/components/ScreenScale.vue` | 核心缩放组件 |
| **新增** | `src/composables/useChartScale.js` | ECharts 自适应工具 |
| **修改** | `src/App.vue` | 引入 ScreenScale 包裹路由 |
| **修改** | `src/views/Dashboard.vue` | ECharts fontSize 动态化 + CSS 优化 |
| **修改** | `src/views/Analytics.vue` | ECharts fontSize 动态化 + CSS 优化 |
| **修改** | `src/views/EnergyTrend.vue` | ECharts fontSize 动态化 + CSS 优化 |
| **修改** | `src/three/core/index.js` | 3D 场景大屏适配 |
| **修改** | `src/layouts/MainLayout.vue` | 侧边栏宽度弹性化 |

**总计**：3 新增 + 6 修改，约 300 行核心代码。
